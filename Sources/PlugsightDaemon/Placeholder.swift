// Placeholder so the PlugsightDaemon target has at least one source and builds.
// Real content (Collector, Scorer, Scanning, API, and the platform-specific
// DeviceEventSource implementations) arrives in later nodes (N8+).

/// Namespace for the Plugsight daemon. Intentionally empty for now.
public enum PlugsightDaemon {}
